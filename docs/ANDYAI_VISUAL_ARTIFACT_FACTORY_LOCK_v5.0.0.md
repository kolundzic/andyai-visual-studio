

AndyAI Visual Studio now crosses from template marketplace into visual artifact factory.


Prompt Assistant → Template Wizard → SVG → HTML → README Pack → Diagram Pack → Bundle ZIP → Evidence Metadata → QA Inspector.


No artifact is considered ready until it has a human-readable preview, machine-readable metadata, and QA visibility.
